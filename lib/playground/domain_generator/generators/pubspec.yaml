name: generators
description: Getters and setters to save variables to and from Map
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.5.0-180.3.beta <4.0.0'

dependencies:
  build:
  source_gen:
  annotations:
    path: ../annotations/

dev_dependencies:
  build_test:
  build_runner:
  test:
  
