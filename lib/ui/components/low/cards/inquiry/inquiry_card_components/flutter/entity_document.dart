import 'package:flutter/material.dart';

class EntityDocument<T> extends StatelessWidget {
  const EntityDocument({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
